"use client";import Link from"next/link";import{useEffect,useState}from"react";import{apiFetch}from"../../lib/api";type N={id:string;title:string;body:string;category:string;severity:string;read_at?:string;created_at:string};export default function Page(){const[rows,setRows]=useState<N[]>([]);async function load(){const r=await apiFetch('/notifications/');if(r.ok){const b=await r.json();setRows(b.results??b)}}useEffect(()=>{load()},[]);async function read(id:string){await apiFetch(`/notifications/${id}/read/`,{method:'POST'});load()}return <main className="adminPage"><div className="pageHead"><div><Link href="/">← داشبورد</Link><h1>اعلان‌ها</h1><p>اقدامات معوق، تخصیص‌ها و رویدادهای GRC</p></div></div><section className="cardsList">{rows.map(x=><article key={x.id} className={`panel notificationCard ${x.read_at?'read':''}`}><div className="roleTitle"><strong>{x.title}</strong><span className="pill">{x.category}</span></div><p>{x.body}</p><small>{new Date(x.created_at).toLocaleString('fa-IR')}</small>{!x.read_at&&<button onClick={()=>read(x.id)}>خوانده شد</button>}</article>)}</section></main>}
