"use client";import Link from"next/link";import{useEffect,useState}from"react";import{apiFetch}from"../../lib/api";export default function P(){const[r,s]=useState<any[]>([]);useEffect(()=>{apiFetch('/control-tests/').then(async x=>{if(x.ok){const b=await x.json();s(b.results??b)}})},[]);return <main className="adminPage"><Link href="/">← داشبورد</Link><h1>آزمون کنترل‌ها</h1><section className="panel"><table className="dataTable"><thead><tr><th>کنترل</th><th>آزمون</th><th>تناوب</th><th>Automation</th><th>فعال</th></tr></thead><tbody>{r.map(t=><tr key={t.id}><td>{t.control_code}</td><td>{t.title}</td><td>{t.frequency}</td><td>{t.automation_type}</td><td>{t.is_active?'بله':'خیر'}</td></tr>)}</tbody></table></section></main>}