"use client";import Link from "next/link";import{useEffect,useState}from"react";import{apiFetch}from"../../../lib/api";
type S={id:string;suggestion_type:string;proposed_value:any;confidence?:string;status:string;created_at:string};
export default function Page(){const[rows,setRows]=useState<S[]>([]);const[msg,setMsg]=useState('');async function load(){const r=await apiFetch('/ai/suggestions/?status=pending');if(r.ok){const b=await r.json();setRows(b.results??b)}}useEffect(()=>{load()},[]);async function review(id:string,status:string){const r=await apiFetch(`/ai/suggestions/${id}/review/`,{method:'POST',body:JSON.stringify({status})});if(r.ok){setMsg('ثبت شد.');load()}else setMsg('ثبت بررسی ناموفق بود.')}return <main className="adminPage"><div className="pageHead"><div><Link href="/ai">← دستیار AI</Link><h1>Inbox پیشنهادهای AI</h1><p>هر پیشنهاد باید توسط انسان Accept، Modify یا Reject شود.</p></div></div>{msg&&<p className="message">{msg}</p>}<section className="panel"><table className="dataTable"><thead><tr><th>نوع</th><th>پیشنهاد</th><th>اطمینان</th><th>اقدام</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.suggestion_type}</td><td><pre className="jsonPreview">{JSON.stringify(x.proposed_value,null,2)}</pre></td><td>{x.confidence??'—'}</td><td><div className="headActions"><button onClick={()=>review(x.id,'accepted')}>Accept</button><button onClick={()=>review(x.id,'rejected')}>Reject</button></div></td></tr>)}</tbody></table>{!rows.length&&<p className="muted">پیشنهاد Pending وجود ندارد.</p>}</section></main>}
